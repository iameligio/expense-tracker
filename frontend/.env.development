# Local development (npm run dev, behind Valet at https://expense-tracker.test).
# Empty base = call same-origin /api, which Vite proxies to the Go backend.
# NOTE: Vite embeds VITE_* vars into the public bundle — never put secrets here.
VITE_API_BASE_URL=
