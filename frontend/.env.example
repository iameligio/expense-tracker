# Reference for frontend environment variables (Vite).
# VITE_* vars are PUBLIC — they are compiled into the client bundle. No secrets.

# Base URL for API calls. Empty = same-origin /api (via Vite proxy in dev, or a
# reverse proxy in prod). Set a full URL only if the API is on another host.
VITE_API_BASE_URL=
