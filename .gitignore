# Dependencies
node_modules/

# Backend env files hold secrets — never commit them (only .env.example is tracked)
backend/.env
backend/.env.local
backend/.env.production

# Any *.local override (frontend Vite convention) and machine-local env
*.local
.env

# Build output
frontend/dist/
backend/server
backend/bin/
# (backend/server is the built binary; cmd/server/ source is tracked)

# Logs & OS cruft
*.log
.DS_Store

# Tooling artifacts
.playwright-mcp/
