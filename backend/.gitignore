# Env files hold secrets — never commit them. Only .env.example is tracked.
.env
.env.local
.env.production
.env.*.local

*.log
/tmp/
# Built binary only (anchored so it doesn't match the cmd/server/ source dir)
/server
bin/
